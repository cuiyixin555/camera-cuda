common --announce_rc

# Convenient flag shortcuts.
build --flag_alias=enable_cuda=@camera_cuda//cuda:enable
build --flag_alias=cuda_archs=@camera_cuda//cuda:archs
build --flag_alias=cuda_compiler=@camera_cuda//cuda:compiler
build --flag_alias=cuda_copts=@camera_cuda//cuda:copts
build --flag_alias=cuda_runtime=@camera_cuda//cuda:runtime

build --enable_cuda=True

# Use --config=clang to build with clang instead of gcc and nvcc.
build:clang --repo_env=CC=clang
build:clang --@camera_cuda//cuda:compiler=clang

# https://github.com/bazel-contrib/rules_cuda/issues/1
# build --ui_event_filters=-INFO
